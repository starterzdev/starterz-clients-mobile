import 'package:flutter/material.dart';

class MyProjectDashboard extends StatelessWidget {
  const MyProjectDashboard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('My Project'),
      ),
    );
  }
}
